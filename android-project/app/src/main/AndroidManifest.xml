<?xml version="1.0" encoding="utf-8"?>
<!-- 
    PlayWell Android Manifest (Zero Internet Permissions, Zero Telemetry)
    Targeting Android 10+ (API 29+) up to Android 14 (API 34+)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.playwell.music">

    <!-- Scoped Storage permissions for local files -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    
    <!-- Android 13+ Granular Media Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Biometric Authentication for Locked Folders / Private Vault -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Media3 Foreground Service & Notification Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- NO INTERNET PERMISSION IN ACCORDANCE WITH STRICT PRIVACY MANDATE -->

    <application
        android:name=".PlayWellApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PlayWell"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayWell"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PlayWell">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Support view intent for outside audio files without logging to library -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <!-- Media3 Playback Service running in its own foreground process constraints -->
        <service
            android:name=".playback.PlayWellMediaService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <!-- WorkManager Hilt integration initializer -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            xmlns:tools="http://schemas.android.com/tools"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup.Initialization"
                tools:node="remove" />
        </provider>

    </application>
</manifest>
